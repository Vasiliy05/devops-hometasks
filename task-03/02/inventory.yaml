all:
  vars:
    ansible_connection: lxc
    ansible_become: yes
  children:
    c1-static:
      vars:
        ansible_lxс_remote: localhost
      hosts:
        c1-static:
    c2-php:
      vars:
        ansible_lxс_remote: localhost
      hosts:
        c2-php:
